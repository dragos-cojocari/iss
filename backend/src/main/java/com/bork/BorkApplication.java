package com.bork;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * BORK (Book Organization & Rental Kiosk) - Main Application
 *
 * Spring Boot application entry point for the BORK library management system.
 */
@SpringBootApplication
public class BorkApplication {

    public static void main(String[] args) {
        SpringApplication.run(BorkApplication.class, args);
    }
}
